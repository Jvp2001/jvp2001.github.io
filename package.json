{
  "files": [
    "src/"
  ],
  "scripts": {
    "start": "parcel serve src/index.html",
    "build": "cross-env NODE_ENV=production parcel build src/index.html --public-url .",
    "deploy": "rm -rf && gh-pages -d src",
    "deloy and run": "npm run build && npm run deploy"
  },
  "homepage": "https://jvp2001.github.io",
  "devDependencies": {
    "cross-env": "^5.2.0",
    "gh-pages": "^5.0.0",
    "parcel": "^2.0.1",
    "typescript": "^5.0.4"
  }
}
